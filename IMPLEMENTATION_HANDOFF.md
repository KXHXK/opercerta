# FlowOps 实施交接

## 当前检查点

- 书面设计已经总审通过并冻结为实施基线。
- 仓库尚无业务代码、依赖锁文件、部署配置和远程 GitHub 地址。
- 下一次对话从实施规划与最小可运行纵向闭环开始，不沿用旧的简陋 Agent 实现或旧实施计划。

## 新对话必须先做

1. 完整阅读 `README.md` 以及 `docs/specs/` 下的三份设计文件，顺序为总体设计、组合设计、FlowOps 详细设计。
2. 以详细设计为业务与架构基线，先核验 LangGraph、FastAPI、Pydantic、FastMCP 等依赖的当前官方用法和兼容版本，再形成可逐项验收的实施计划。
3. 只实施 FlowOps；用测试驱动完成“事件接入 → 证据采集 → 处置建议 → 人工审批 → 幂等写入 → 审计与恢复”的业务闭环。
4. 每个效果数字都保留基线、测试数据、测量脚本和结果证据；指标未测出前使用目标值或空值，不写成已实现结果。
5. 使用公开或合成数据，从零编写全部代码和文档，不导入任何原单位源码、数据、截图、模型、品牌或内部规则。

## 第一阶段完成条件

- 非法输入、状态恢复、审批竞态和幂等写入测试先于对应实现并可重复运行。
- 最小纵向闭环能够在本地 Docker 环境启动，失败路径和人工接管路径可演示。
- README、架构图、接口说明、评测报告、部署与回滚说明随实现同步更新。
- 通过详细设计中的发布门禁后，再部署公开演示、填写在线地址并开始 SecEvidence。

## 可复制到新对话的启动语

> 工作目录为本 FlowOps 仓库根目录。请先完整读取 `README.md`、`IMPLEMENTATION_HANDOFF.md` 和 `docs/specs/` 下三份设计基线；核验当前官方依赖版本后，先给出可执行实施计划，再按测试驱动从非法输入、状态恢复、审批竞态和幂等写入开始实现。严格只实施 FlowOps，不复用旧公司材料，不虚构指标，未通过发布门禁前不启动其他项目。

