# 2026-07-20 开发日志

## 四份正式设计与当前路线复核

- 重新完整读取命名设计、四项目总体设计、作品集组合设计和 OperCerta 详细设计。
- 当前实现已真实使用 WSL2 Ubuntu、Docker Compose、FastAPI、Pydantic、LangGraph、FastMCP/MCP、PostgreSQL、React/SSE 和 GitHub Actions；审批竞态、幂等写入、重启恢复、审计和补货固定评测已有证据。
- 发现当前纵向业务只完成库存补货：设备对象虽存在于请求枚举，但 API 失败关闭，`equipment.get_status`、维修规则、维修工单、设备前端和设备评测均未实现；“作业异常”只存在于总体定位，详细设计未定义独立对象和工具。
- 发现共同基线仍有真实缺口：Redis 只有 Python 依赖而未进入 Compose/缓存路径，OpenTelemetry 未实现，当前只有 request ID 和 Prometheus 应用指标；模型仅支持 Mock；公网只有静态专题，没有可交互 HTTPS 后端；Release Tag、真实模型代表性验证和完整学习交付仍未完成。
- 最新作品集记录提交 `bbb67c9` 的 main GitHub Actions run `29682353256` 已完成并成功；仓库工作区只保留用户自己的 `.gitignore` 修改。

## 三业务与学习交付决策

- 用户确认 OperCerta 必须实现三业务，并要求所有已设计的核心技术栈有真实应用与可讨论证据。
- 第三业务确定为“合成作业任务阻塞/超时 → 异常恢复工单”，与库存补货、设备维修共同形成三条纵向闭环。
- 新设计采用一套共享 LangGraph 可靠性内核和三个类型化场景适配器，不复制审批、幂等、恢复和审计逻辑，也不增加自由讨论型多 Agent。
- 原五工具契约修订为六个固定 MCP 工具，新增 `task.get_status`；`policy.list_constraints` 和 `work_order.create` 通过判别式类型支持三业务。
- 项目完成标准加入中文核心技术手册、手动实验手册、面试讲解、真实故障案例和用户亲手复现要求。写文档不替代运行、故障实验和口头解释。
- 正式修订文件为 `docs/superpowers/specs/2026-07-20-opercerta-three-business-release-design.md`；用户已完成书面复核并确认。

## 三业务实施计划

- 使用 `writing-plans` 将批准规格拆成八项连续 TDD 任务：通用领域/迁移、共享 LangGraph 内核、设备维修、作业恢复、三业务前端、Redis/OpenTelemetry/Real model、跨业务评测/Compose、发布证据与中文学习包。
- 执行方式沿用用户此前选择的 Inline Execution，不启用 subagent；在共享内核、三业务后端、发布门禁和最终交付设置复核点。
- 计划文件为 `docs/superpowers/plans/2026-07-20-opercerta-three-business-release.md`。计划只定义预期命令与结果，新增通过数量和性能数字仍不存在。

## 三业务后端与单页控制台实施

- Task 1–4 已依次完成通用契约/迁移、共享受控动作入口、设备维修闭环和作业恢复闭环。实现提交为 `3b00bf2`、`a180c6d`、`27d5134`（Task 1 的迁移与契约包含在此前已核验主线提交中）。
- Task 5 先以失败测试证明前端仍重建库存专用审批字段、只发送库存对象；实现后审批绑定改为判别联合并原样回传，三业务共享同一详情、审批与审计区域。
- 对照批准规格时发现实现偏差：设计要求三对象都支持只读 `query`，但 API 和场景注册表仍只允许 `create_work_order`。新增三对象 API 测试先得到 `3 failed / 422`，随后依次定位场景注册表、查询评估落库和非动作结果白名单三个封闭边界。
- 查询路径现为“真实 MCP 取证 → 确定性评估 → 报告 → completed”，不调用模型解释、不创建审批或工单；页面每个场景提供“查询状态”和“创建处置”两个真实动作。
- 浏览器检查覆盖桌面三栏、三场景切换、390px 单栏和无横向溢出；浏览器 console 无 warning/error。专题主标题进一步收小，公开展示移除过期的 `Private GitHub` 与“设备未完成”表述。
- 诊断期间失败 API 测试在专用 `opercerta_test` 留下未登记清理的 operation，导致全局恢复扫描测试捞到 6 条旧记录。确认数据库名后只清理测试库 operation；目标恢复测试恢复为 `1 passed`。
- 清理后新鲜总门禁：后端 `392 passed in 95.98s`；Ruff 全绿、125 文件格式正确、mypy 检查 59 个源文件无问题；前端 12 个测试文件/25 条测试通过，TypeScript/Vite 生产构建成功。

## Task 6：Redis、OpenTelemetry 与真实模型适配器

- 先写缓存故障旁路、JSON/TTL、严格模型输出、脱敏 span、生产配置和 Redis Compose 契约测试；RED 阶段因缓存、模型和追踪模块尚不存在而在收集阶段失败，证明测试先于实现。
- Redis 只缓存初次或查询取证。缓存键带固定命名空间、证据类别和参数哈希，值保留来源/规则版本并设置短 TTL；缓存连接、解析或写入失败只记录低基数 `hit/miss/write/error`，随后直连 MCP。批准后的 `revalidate` 使用未缓存 gateway，集成测试用“初始快照不变、真实 MCP 事实变化”证明仍以 `approval_snapshot_mismatch` 安全失败。
- OpenTelemetry 默认关闭；启用时使用 OTLP HTTP exporter。API 请求、三个场景的 LangGraph 节点、MCP 读写、Redis get/set 与 SQLAlchemy 执行建立 span，API→MCP 通过 W3C trace context 传播。属性采用明确 allowlist，不记录 JWT、API key、SQL、参数、Prompt、证据正文或用户消息。
- Real model 采用 OpenAI-compatible `/chat/completions`，只接受严格 `summary`/`rationale` JSON；温度固定为 0，超时固定，最多两次尝试。模型不能决定动作、数量、优先级或权限；真实模式失败不会切换到 Mock 后继续写入。Compose 和 CI 默认仍为 Mock，尚未使用用户密钥产生真实模型结果。
- 官方版本复核后锁定 OpenTelemetry API/SDK/OTLP HTTP `1.44.0`；Redis 官方镜像固定为 `redis:8.8.0-trixie`，内部运行且不映射宿主机端口。Redis 镜像实际拉取和三业务 Compose 验证归 Task 7，当前不得写成已通过。
- 首次总门禁取得 `407 passed in 93.08s`，但同一命令发现两份文件需格式化，而且 `.venv\\Scripts\\python.exe -m uv` 不可用。执行 Ruff 格式化后改用全局 `uv lock --check`；最终在全部修改后的工作树重跑完整测试为 `407 passed in 102.64s`，锁文件、Ruff、131 文件格式和 mypy（62 个源文件）通过。
- 提交前安全审查发现 OpenTelemetry `start_as_current_span` 默认会把未处理异常的 message 与 stacktrace 写进 span，可能违背“无 token/正文/traceback”边界；同时指标 observer 自身异常可能让缓存旁路再次失败。新增两个 RED 用例真实复现后，Trace 改为关闭自动异常记录、只保存异常类型和 ERROR 状态，缓存指标改为完全 best-effort。Real adapter 同时加入固定 `max_tokens=256` 预算上限；修复后最终完整测试为 `409 passed in 101.15s`。

## Task 7：跨业务评测、Compose 与性能矩阵

- 新套件 `opercerta-three-business-v1` 继承原 30 条库存用例，再冻结 6 条设备和 6 条作业用例；新增用例检查实际 MCP 工具、终态、审批、工单和审计事实。真实 API/MCP/PostgreSQL 边界运行 42 passed、0 failed。
- Compose smoke 扩展为三业务批准写入、设备拒绝零写入、重复审批冲突和数据库断言；同一 WSL 会话重启 API/MCP 后，作业待审批 operation 仍可恢复且保持零审批/零工单。
- 首次性能编排因 PowerShell/WSL/Bash 嵌套引号在写报告前失败，随后固化为 `scripts/run_performance_matrix.sh`。第一次报告 MCP 调用为 0；对比 Redis 指标和镜像版本后确认复用了 Task 6 旧镜像，脚本增加强制 `--build` 并由资产测试保护。
- 最终 2×2 矩阵 12 格、60/60 个 query operation completed、零错误。禁用缓存每场景 10 次 MCP/0 hit；启用时 2 次 MCP/8 hit。每格只有 5 次，本机延迟不解释为生产指标或并行优势。
- 首次总回归出现一次设备 MCP 503；该 operation 已在数据库创建并进入待审批，但旧 harness 只在 HTTP 202 后登记清理 ID，继而污染恢复扫描。确认专用测试库与精确 UUID 后清理 operation/checkpoint，并用 `TrackingOperationRepository` 在创建成功时立即登记。聚焦 17 条通过；最终新鲜总门禁为 414 passed in 103.80s，锁文件、Ruff、134 文件格式、mypy 62 个源文件和仓库安全扫描通过。
- 完整数值和命令见 `docs/release-evidence/three-business-evaluation-compose.md`。
