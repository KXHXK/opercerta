# 2026-07-19 开发日志

## 公开专题最终本地门禁

- 在隔离 worktree `feature/public-portfolio-showcase` 继续执行已批准的公开作品集计划；个人作品集的 OperCerta 入口仍保持未暂存、未提交、未部署，未吸收 `D:\CODEX\resume\portfolio` 中任何既有用户 hunk。
- 第一次完整门禁中，后端 `342 passed in 73.22s`，随后 Ruff 在 `tests/unit/runtime/test_static_hosting_assets.py` 报 `I001`，因此整条门禁按失败处理，没有提前推送。
- `ruff check --diff` 精确证明根因是唯一 import 与模块常量之间多一行空白，不是依赖或导入顺序问题。只删除该空行后，单文件 Ruff、format check 和静态托管契约 `3 passed`。
- 对当前精确工作树从头重跑完整门禁：后端 `342 passed in 65.94s`；Ruff `All checks passed!`；105 个文件 format check 通过；mypy 检查 50 个源文件无问题；仓库安全扫描通过。
- 前端门禁为 11 个测试文件、24 条测试通过；Vite 生产构建成功，产物仍为 `index-CUPW156B.js` 与 `index-0pGkm15M.css`，和已验证的 Netlify 线上资源指纹一致。
- 本地门禁证据提交为 `811885e`。功能分支推送后创建 Draft PR #4；PR run `29652818349` 的四个快速 job 全部成功，`compose-smoke` 按设计跳过。
- 经用户确认，PR #4 转为 Ready 并按仓库既有 merge-commit 策略合并为 `0f262e0`。GitHub 合并性检查在写入前为 `mergeable=true`、`mergeable_state=clean`，没有 rebase 或 force push。
- 合并后的 main run `29652991288` 五个 job 全部成功；其中 `compose-smoke` job `88102487952` 完成 Compose 业务 smoke、API/MCP 重启恢复与无条件清理。
- GitHub 插件能识别当前用户但对 Private 仓库仍返回 404。没有把它误判为仓库不存在；分支上传使用 Git Credential Manager，PR/API 操作中的凭据只存在于单个 PowerShell 进程且未输出或写入项目。
- OperCerta 原产品 release gate 仍为 `CLOSED`；静态专题上线不代表生产身份、公开后端、自动部署或完整浏览器 E2E 已完成。

## 作品集 Netlify 静态镜像

- 既有 Sites 作品集虽然平台侧已有部署，但 2026-07-19 公网复验仍返回 HTTP `403`。为避免把平台部署成功误写成可访问，按已确认规格建立独立静态镜像，不修改 Sites 源作品集的内容边界。
- 镜像仓库实际放在 `D:\CODEX\agent-portfolio\portfolio-netlify`。原计划目录位于 `D:\CODEX\resume`；受限权限环境在该目录写入失败后，只复制到工作区内继续，没有删除原目录或覆盖用户文件，并在规格和计划中记录路径修订。
- Windows Node.js `24.18.0` 直接 `spawn("npm.cmd")` 返回 `EINVAL`。目标测试先因 `resolveNpmInvocation` 缺失而 RED；最小修复改为使用当前 `node.exe` 直接执行 npm CLI JavaScript 入口，避免 `shell: true`，回归变为 `6/6` GREEN。
- `npm run verify` 完整通过：重新构建 `D:\CODEX\resume\portfolio`，执行 SSR 静态导出，生成 15 个文件；HTML 中 OperCerta 精确 URL 出现一次，7 个本地资源引用全部存在，缺失数为 0。
- Windows 端 Netlify CLI 登录身份与团队 `KXH` 已确认；WSL 中 CLI 缺失，因此使用 npm registry 当前 `netlify-cli@26.2.0` 的一次性 `npx` 执行，不全局安装。
- 新站点 `kxh-agent-portfolio` 已创建，site id 为 `bb607935-3c7f-45a2-b527-dcfd625d8ce7`，与 `opercerta-kxh` 独立。preview deploy `6a5c814643be8e4160b17def` 和 production deploy `6a5c8184ffb46a40d1b49b6d` 均成功。
- 生产地址 <https://kxh-agent-portfolio.netlify.app> 实测为 `200 text/html; charset=UTF-8`，标题为 `KXH — AI Agent / 大模型应用开发工程师`，包含 OperCerta URL、`target="_blank"` 与 `rel="noreferrer"`；目标专题 <https://opercerta-kxh.netlify.app> 同轮返回 200。浏览器语义快照确认作品集正常渲染并显示第 04 张 OperCerta 卡片。
- 本轮只发布静态作品集镜像；没有新增 Function、API、数据库、密钥或公开写入口。`OperCerta production release gate: CLOSED` 保持不变。

## 作品集单页优化与生产替换

- 用户明确要求取消 `/#projects` 式导航、缩小标题、重排视觉并把四项目按 OperCerta、ForenTrail、SiteVerum、Federune 展示。实现采用连续单页，不含内部 hash 链接或分页状态。
- 四项目内容直接来自总体设计规格。OperCerta 标记为“公开专题已上线 · 产品门禁关闭”；其余三项标记“规划中 · 未启动”，无虚构链接、完成度或指标。
- 用户明确授权公开邮箱 `shepherdlaurie238@gmail.com` 与手机号 `15850024580`。总体设计原规则“不公开手机号”因此由本次用户产品决策覆盖；页面只公开用户主动提供的联系方式。
- 源作品集契约先观察到 3 条失败，重构后为 3/3 通过；镜像新增禁止 hash 导航与必需公开内容失败关闭测试，8/8 通过。真实导出和 Vinext 生产构建成功。
- 桌面端主标题实测约 `66.56px`，移动端约 `42.944px`；两种视口均无横向溢出，根 URL 的 hash 为空。
- preview deploy `6a5c92ca65ca5b75486b82d1` 通过后，production deploy `6a5c986587eaef5b3156f49b` 已上线。生产地址 <https://kxh-agent-portfolio.netlify.app> 返回 200，四项目顺序、邮箱、电话和 GitHub 均通过；HTML 中内部 hash 链接数为 0。
- GitHub 公共 API 同轮确认 `KXHXK/opercerta` 为 `public`，仓库首页返回 200。现有 main branch protection 端点仍返回 404，表示尚未配置保护规则，不再沿用“Private 账户能力限制”的解释。
