# 2026-07-23 开发日志

## 目标

发布 Agent 核心 feature branch，建立 Draft PR，并用真实 GitHub Actions 复验仓库安全、Python 质量、后端和前端门禁。生产门禁未满足前继续只实施 OperCerta，不启动 ForenTrail。

## 已完成

- 推送 `feat/agent-core-implementation`，创建 [Draft PR #8](https://github.com/KXHXK/opercerta/pull/8)。
- 首次远程 run `29936292055` 中 repository-safety、python-quality、frontend 通过，backend-tests 在 Alembic 迁移阶段失败。
- 按 RED/GREEN 修复 CI 数据库镜像：先让资产测试要求 `pgvector/pgvector:0.8.2-pg18-trixie` 并观察失败，再把 backend-tests 的服务镜像从普通 `postgres:18` 改为与本地 Compose 相同的 pgvector 镜像。
- 修复提交为 `ba53e70 fix: use pgvector in backend CI`；定向 12 条 CI/容器资产测试、Ruff、format、仓库安全和 diff 检查均通过。
- 最新基线 run `29937375023` 的 repository-safety、python-quality、backend-tests、frontend 全部通过；`compose-smoke` 按工作流规则在 PR 事件跳过，只在 `main` 或手动触发运行。
- 用真实 Kimi + RAG 复现第二轮规划重复调用已完成工具的问题。根因不是 Harness 误判，而是 replan 仍向模型暴露全部工具；修正为只暴露尚缺证据的工具，保留重复调用拒绝规则。修复后直接 LangGraph 代表探针按 `inventory → knowledge → policy` 完成。
- 给 operation runner 增加异常原子收口：图或 provider 抛出异常时，同一 operation 写入固定 `dependency_unavailable` 终态；即使失败收口自身异常，也保留原始异常而不记录 provider 文本。
- 给真实模型验证器增加有界失败证据，只允许 stage、HTTP 状态、固定错误码和 operation 状态进入报告，不保存响应正文、模型原文或凭据。
- 新鲜本地回归：unit `352 passed`、关键 Agent 图集成 `7 passed`、Ruff 通过、`188 files already formatted`、Mypy 76 个源文件通过、`git diff --check` 通过。
- 首次推送本轮修复后，run `29945720955` 为 backend-tests `572 passed / 1 failed`；失败仅因证据文档替换新诊断报告路径时遗漏规格锁定的历史路径。保留原测试契约、补回历史路径后，提交 `221fe3d`；run `29946792369` 的 repository-safety、python-quality、backend-tests、frontend 全绿，完整后端 `573 passed in 61.77s`、三业务评测 `1 passed`、Agent 评测 `9/9`。
- 用户轮换本地 PostgreSQL 角色密码后，首次安全探针确认主仓库与 worktree ignored 配置一致，但 WSL 连接 Windows loopback 被拒绝；这不是认证失败。改用 Windows 原生 `psql` 后发现配置尚未同步，用户以隐藏输入和 URL 编码更新主仓库 `.env.local`，最终 `SELECT 1` 安全探针退出码 0。全过程未输出密码或完整连接串。
- 合并前 inline 审查没有发现 Critical/Important 问题：异常收口不能覆盖终态，replan 工具裁剪仍受 Harness 约束，真实模型报告只保存允许字段。approved-path 细分诊断列为非阻塞后续改进。

## 问题与解决方法

### PR 后端测试缺少 vector extension

- **现象：** 后端测试不是业务断言失败，而是在迁移 `CREATE EXTENSION IF NOT EXISTS vector` 时退出。
- **根因：** 产品和本地 Compose 已使用 pgvector，但 GitHub Actions backend-tests 仍使用普通 PostgreSQL 18；代码与 CI 基础设施发生漂移。
- **修复：** 用资产契约锁定与 Compose 相同的 `pgvector/pgvector:0.8.2-pg18-trixie`，禁止回退到 `postgres:18`，再修改工作流。
- **验证：** 本地 RED/GREEN 与修复后的远程 run 同时通过。这个修复不放宽迁移、不跳过 RAG 测试，也不伪造 extension。
- **面试表达：** “本地通过而 CI 失败时，我先比较运行环境。迁移明确依赖 vector extension，根因是 CI 镜像漂移；我先写资产测试固定基础设施契约，再统一镜像并用远程 Actions 复验。”

### Kimi replan 重复已完成工具

- **现象：** 开启 RAG 后，Kimi 第一轮已取得库存和知识证据，第二轮却再次选择已完成工具，Harness 按契约返回 `duplicate_tool_call`，遗漏 policy。
- **根因：** 图虽然知道缺少哪类证据，但 replan 仍把完整工具集交给模型，给了模型重复选择的空间。
- **修复：** 根据已经持久化的 observation 计算 missing tools，下一轮只暴露尚未完成的工具；如果没有可用工具但证据仍不完整，则显式失败，不进入无界循环。
- **验证：** RED 先证明第二轮仍暴露 inventory + policy；GREEN 后 16 条定向测试通过，真实 RAG 图探针按 inventory、knowledge、policy 顺序完成。
- **面试表达：** “Harness 负责拒绝越界，Graph 负责缩小下一轮行动空间。只做重复调用拦截会让任务失败；只靠 Prompt 又不可靠，所以我同时保留硬约束并按状态裁剪工具集。”

### provider 异常后的 operation 原子收口

- **现象：** 图或 provider 在创建 operation 后抛异常时，记录可能停留在 `received`，重启恢复只能把它转成冲突失败。
- **根因：** operation 已经持久化，但图调用异常没有经过业务终态仓储。
- **修复：** runner 捕获异常后以固定安全错误写入 `failed`；收口失败时记录不含异常正文的固定日志，并重新抛出原始异常。
- **验证：** RED/GREEN 覆盖正常失败收口、provider 文本不落库、二次收口失败仍保留原始异常。

### 真实模型端到端仍受外部延迟阻塞

- **观察：** 一次完整 Compose inventory query 在约 21 秒内得到 202、`completed` operation 和 Agent Trace 200，但旧验证器只留下通用 `AssertionError`；增强安全诊断后，下一次代表调用在约 30 秒返回 `503 dependency_unavailable`。
- **判断：** 低层 native Tool Calling、无 RAG 图、修复后的有 RAG 图均已通过；最终 Compose 结果仍不是稳定成功证据。最后一次失败位于 create operation 的外部依赖有界超时，而不是数据库、MCP 健康或 operation 悬挂。
- **边界：** 不继续盲目消耗真实调用。报告只写 `create_operation / 503 / dependency_unavailable`；不能声称 Real Kimi 端到端通过，也不能用 Mock 结果替代。

### 本地测试凭据再次进入失败 traceback

- **现象：** 尝试本地完整数据库测试时，连接失败 traceback 展开了本地 PostgreSQL 测试角色凭据。
- **处置：** 该值未写入代码、Git 或文档，但按已暴露凭据处理；用户已在 Windows PostgreSQL 手动轮换角色密码，并同步 ignored `.env.local`。
- **验证：** 主仓库与 worktree 配置一致；Windows 原生 `psql` 使用 ignored 配置执行 `SELECT 1` 成功。只记录布尔结果，不记录旧值、新值或连接串。

### 证据文档路径也属于可执行发布契约

- **现象：** 产品和静态门禁均通过，但完整 backend-tests 因证据文档缺少 `tmp/real-model-agent-v1-report.json` 失败。
- **根因：** 新增 diagnostic 报告时直接替换了历史报告路径，破坏了冻结交付资产测试；不是业务实现回归。
- **修复：** 不修改、不放宽测试，文档同时声明历史报告和新诊断报告的用途与 ignored 边界。
- **验证：** 本地先复现同一 RED，修复后定向测试与仓库安全通过；远程 run `29946792369` 完整后端 573 条及两套评测通过。
- **面试表达：** “文档里的证据入口也是发布接口。新增诊断格式时应保持旧证据可追溯，而不是让最新文件覆盖历史契约。”

## 当前边界与下一步

- PR #8 保持 Draft，尚未合并；主线 `compose-smoke` 尚未产生新 Agent 核心证据。
- Real Kimi Tool Calling 的重复工具规划和 provider 异常 operation 原子收口已经修复并有本地证据；完整 Compose 真实模型路径仍未稳定通过。
- 本轮修复的 Draft PR 快速 Actions、inline review 与凭据轮换验证已完成；下一步做用户手动演示/口述掌握检查、PR 合并审批和主线门禁。
- `OperCerta production release gate: CLOSED`。
