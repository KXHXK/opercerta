# 2026-07-16 开发日志

## 00:09 Asia/Shanghai｜Codex 工作区写权限恢复

- 目标：确认重启后能继续在 OperCerta 仓库写文档并执行 Git 提交。
- 观察：重启前本机进程启动报 `CryptUnprotectData failed: 2148073483`，受管权限还将 `.git` 暴露为只读；当时所有写入尝试均失败，仓库未发生变更。
- 动作：分别验证 PowerShell 启动、仓库根目录临时文件写入和规范化 `.git` 路径下的临时文件写入；所有探针均在验证后删除。
- 证据：探针输出为 `PowerShell=ok`、`WorkspaceProbe=True`、`GitWriteProbe=True`；仓库为 `D:\CODEX\agent-portfolio\opercerta`，分支为 `main`，清理后 `git status --short` 无输出。
- 结果：当前任务具有继续写规格和提交 Git 所需的权限，不需要用户再修改设置。
- 风险与下一步：权限恢复不代表发布通过；继续完成已批准的 Task 4 书面规格，不提前写实现代码。

## 00:18 Asia/Shanghai｜Task 4 幂等工单规格落盘

- 目标：补齐可靠性内核计划引用但未精确定义的工单命令、记录、写入结果、稳定错误和 canonical JSON 边界。
- 观察：现有数据库已具备 `work_orders` 表、operation 与幂等键唯一约束；计划已规定行锁、approved 授权、十路竞态和唯一冲突翻译，但领域字段及非法输入边界仍需冻结。
- 动作：按用户确认的方案 1 编写 `docs/superpowers/specs/2026-07-16-work-order-idempotency-contract-design.md`，固定新工单状态为 `created`，并明确有效一次、同事务审计、安全重放、同键异参和非目标。
- 证据：规格自审未发现 TBD、TODO、未选择方案或跨 Task 范围；本次只修改文档，未创建生产代码、测试或数据库记录。
- 结果：Task 4 已有可供用户逐条复核的书面契约。
- 风险与下一步：书面规格尚未得到用户最终复核；在确认前不进入 writing-plans 或 TDD，发布门禁保持 `CLOSED`。

## 00:27 Asia/Shanghai｜Task 4 书面规格确认

- 目标：关闭 Task 4 设计复核门禁，进入可执行计划编写。
- 观察：用户明确回复“确认规格”，没有提出字段、事务顺序、错误语义或测试边界修改。
- 动作：把规格、文档索引、当前状态和交接状态同步为已确认；开始使用 writing-plans 回填现有可靠性计划。
- 证据：已确认规格的落盘提交为 `80b69e8`；确认时生产代码和 Task 4 测试文件仍不存在。
- 结果：可以编写 Task 4 精确实施步骤，但尚未获得任何新测试通过证据。
- 风险与下一步：完成计划自审和提交后，沿用此前选择的 inline execution 从领域非法输入 RED 开始；发布门禁保持 `CLOSED`。

## 09:18 Asia/Shanghai｜Task 4 聚焦 TDD 计划完成

- 目标：把已确认的幂等工单契约转换为无需临场猜测的逐文件 RED/GREEN 实施步骤。
- 观察：现有 `work_orders` Schema 和唯一约束无需迁移；审批竞态测试中的 async Engine fixture 需要上移到共享 `tests/integration/conftest.py`，供两个数据库测试模块复用。
- 动作：创建 `docs/superpowers/plans/2026-07-16-work-order-idempotency.md`，分为领域契约、共享 fixture、原子 Repository 与并发证明、实测证据四个检查点；总计划 Task 4 链接该聚焦计划。
- 证据：规格覆盖标记齐全；计划中 9 个 Python 代码块均通过语法编译，Pydantic `JsonValue` 导入和计划内 JSON 边界烟雾检查成功；`git diff --check` 通过。
- 结果：计划可以按用户已选择的 inline execution 执行；本步骤没有创建生产代码、Task 4 测试、数据库工单或新的通过指标。
- 风险与下一步：计划代码仍必须经过真实 RED/GREEN 验证，不能把语法检查当功能通过；下一步从 `tests/unit/domain/test_work_orders.py` 的非法输入 RED 开始，发布门禁保持 `CLOSED`。

## 09:36 Asia/Shanghai｜Task 4 幂等工单 RED/GREEN 完成

- 目标：实现 PostgreSQL 边界内有效一次的模拟工单创建，并保留完整可复验的测试证据。
- RED：领域测试先因 `IdempotencyConflict`/`WriteNotAuthorized` 缺失退出 1；Repository 集成测试再因 `work_order_repository` 模块缺失退出 1。两次失败均发生在生产实现之前。
- GREEN：领域 focused `27 passed`、单元全集 `56 passed`、审批 fixture 回归 `4 passed`、工单 focused `12 passed`、数据库集成 `17 passed`、完整测试 `73 passed`；Ruff lint、全量 format check 和 mypy（12 个源文件）通过。
- 并发证据：十路相同命令测试由 20 个独立 Pytest 进程执行，实测 `20/20`；每轮断言一次创建、九次重放、一个共享 ID、一行工单和一条创建审计，不解释为生产成功率。
- 诊断与修正：全量 format check 暴露 `b37a659` 的三个既有文件只存在换行差异，机械格式化并以审批/迁移 `5 passed` 复验后提交 `ca8fbb7`；mypy 随后指出 Repository 行类型应为 `RowMapping`，按具体类型修正后整套门禁通过。
- 提交：领域契约 `6f99bf6`；共享 secret-safe Engine fixture `8408f81`；幂等 Repository 与集成测试 `88c014c`。
- 结果：Task 4 在本地 PostgreSQL 事务边界完成，证据归档至 `docs/release-evidence/work-order-idempotency.md`；没有调用真实外部系统，也没有启动 Task 5。
- 风险与下一步：Linux/Docker 和 LangGraph 四点重启恢复仍未验证，发布门禁保持 `CLOSED`；下一步先完成 Task 5 规格复核和 TDD 计划。

## 10:18 Asia/Shanghai｜Task 5 四点重启恢复规格落盘

- 目标：在写实现前冻结 LangGraph 检查点与 PostgreSQL 业务事实共同恢复的精确契约。
- 确认：复用 `operations.request_payload` 保存 `schema_version=1` 最小快照，不新增迁移；审批后重启同时覆盖批准与拒绝；新增独立 `OperationStateRepository` 管理状态与审计同事务迁移。
- 动作：分段确认总体架构、正常与四点重启数据流、领域/错误契约、TDD 与非目标；写入 `docs/superpowers/specs/2026-07-16-langgraph-restart-recovery-design.md`。
- 依赖核验：本地 `langgraph-checkpoint-postgres==3.1.0` 的 `from_conn_string` 与 `setup()` 接口符合总计划；源码确认 `LANGGRAPH_STRICT_MSGPACK` 在 `langgraph-checkpoint==4.1.1` 有效，且必须在默认 serializer 构造前设置。
- 自审证据：`git diff --check` 通过；占位文本匹配数为零；快照、五个状态方法、六种恢复动作、严格序列化、三类稳定错误和发布门禁标记均存在。
- 结果：Task 5 已有完整书面规格，但尚未创建生产代码、测试、checkpointer 表或新的通过指标。
- 风险与下一步：书面规格仍需用户复核；确认后使用 writing-plans 编写聚焦 TDD 计划，发布门禁保持 `CLOSED`。

## 10:36 Asia/Shanghai｜复核机制与进度口径调整

- 观察：用户说明无法有效审查 LangGraph、事务和类型等技术规格，只能形式确认；继续逐项要求技术确认会增加时间但不能提供真实质量保障。
- 判断：现有 TDD、原子事务、证据和 Git 检查点方向合理，但可靠性内核先行时间较长，存在推迟用户可见纵向闭环的风险。
- 决策：采用风险分级复核。产品范围、成本、外部账号、安全边界和发布由用户决定；已确认架构内的内部技术细节由 Codex 通过测试、静态检查、自审和日志负责。
- 进度：按当前六项可靠性计划和 Task 5 较大工作量估算，可靠性内核约 60%–65%；按完整设计中的 MCP、API/SSE、前端、评测、安全、Docker/Linux 与公开部署范围估算，完整发布约 25%–30%。两者均为规划估算，不是效果指标。
- 时间：没有证据保证一周内完成全部公开上线门禁；一周内完成可靠性内核和本地最小纵向闭环仍可作为目标，但不能为了日期虚构完整五工具、评测或部署结果。
- 保护措施：Task 6 后冻结可靠性内核，立即转向纵向闭环；后续只在高风险或范围变化时请求用户技术决策。
- 下一步：Task 5 书面规格已确认，直接编写聚焦 TDD 计划并自审，发布门禁保持 `CLOSED`。

## 10:56 Asia/Shanghai｜确认技术文档持续保留

- 用户目标：在不遗留隐患、不保留未实现技术、不为赶工模糊处理的前提下尽快形成业务闭环；同时保留设计、计划、实施方案和验证过程，供以后人工复核、排障、修改、学习与面试复盘。
- 决策：风险分级只减少用户对内部技术细节的形式审批，不减少本地技术文档。A/B/C 级工作仍按风险保留规格、计划、RED/GREEN、故障诊断、数据库断言、静态检查、重启证明、提交和未完成范围。
- 路线解释：Task 1–4 分别建立输入边界、确定性恢复决策、审批原子性和工单幂等性；Task 5 证明跨进程重启安全；Task 6 汇总可复验门禁证据并把工作边界移交到 OperCerta 最小纵向业务闭环。
- 当前事实：本次只补充工程决策与学习记录，不新增生产功能或测试结果；Task 5 聚焦 TDD 计划仍是下一实施步骤，发布门禁保持 `CLOSED`。

## 11:20 Asia/Shanghai｜Task 5 聚焦 TDD 计划完成

- 目标：把已确认的 LangGraph 四点重启恢复契约转换为逐文件、逐 RED/GREEN、可独立提交的实施步骤。
- 文件边界：计划拆分共享 JSON 校验、快照领域模型、`OperationStateRepository`、独立 PostgreSQL checkpointer、JSON-only reliability graph、`RecoveryCoordinator`、A/B 重启矩阵和证据同步；不新增业务迁移。
- 自审：`git diff --check` 通过；规格必需标记齐全；禁止占位文本匹配为零；14 个 Python 代码块均通过语法编译。自审中修正了审批测试初始状态顺序、终态 pending checkpoint 判断、工单定位字段类型验证和 `replayed=True` 验收要求。
- 诊断记录：第一次组合自审命令因 PowerShell 双引号未终止而在解析期失败；读取完整错误并最小复现后，改为不会嵌套冲突的单引号命令，检查成功。该问题没有执行或修改生产代码。
- 结果：计划保存为 `docs/superpowers/plans/2026-07-16-langgraph-restart-recovery.md`；生产代码、测试、checkpointer 表变化和新通过指标仍不存在。
- 下一步：按此前选择的 Inline Execution 从非法快照 RED 开始；发布门禁保持 `CLOSED`。

## 11:38 Asia/Shanghai｜Task 5 前三个 RED/GREEN 与 checkpointer 凭据事件

- 快照领域：测试先因稳定错误缺失而 RED；共享 JSON 边界、`OperationSnapshot`、`ApprovalResume`、恢复读模型和错误完成后，focused `48 passed`、单元全集 `77 passed`，Ruff/format 与 mypy（14 个源文件）通过；提交 `8fb054e`。
- 状态仓储：集成测试先因 Repository 模块缺失而 RED；五个显式状态迁移、恢复视图、安全重复与审计同事务完成后，focused `7 passed`、数据库集成 `24 passed`，Ruff/format 与 mypy（15 个源文件）通过；提交 `5bdacf7`。
- checkpointer：测试先因模块缺失而 RED；首次 GREEN 暴露 SQLAlchemy 把空格编码为 `+`、Psycopg 不按 URL form 规则还原，数据库因此误读 `+search_path`。DSN 回归还证明 `URL.set(password=None)` 不会清除密码。
- 安全事件：上述 Psycopg 失败 traceback 展开了当前本地测试数据库密码。该值没有写入生产代码、测试、文档或 Git；新的 DSN 由非秘密 URL 组件重建，密码只通过临时 `PGPASSWORD` 传递，options 使用 `%20` 编码。
- checkpointer GREEN：focused `4 passed`，与数据库回归合并 `28 passed`，Ruff/format 与 mypy（16 个源文件）通过；提交 `e9b2834`。验证包括独立 `langgraph` Schema、plain JSON 跨 saver 读取和严格 msgpack 前置拒绝。
- 阻塞与下一步：必须再次轮换本地 `opercerta` 测试角色密码并同步 `.env.local`；轮换和无回显复验前暂停 JSON-only graph 实现，发布门禁保持 `CLOSED`。

## 11:49 Asia/Shanghai｜本地数据库密码轮换闭环

- 用户动作：在 PostgreSQL 内轮换 `opercerta` 测试角色密码，并同步更新已忽略的 `.env.local`；新值未发送到对话，也未写入 Git。
- 验证：使用修复后的无密码 checkpointer DSN 执行 `uv run pytest tests/integration/workflow/test_checkpoints.py -q`，新鲜结果为 `4 passed`、退出码 0。
- 意义：旧密码一旦出现在 traceback 就应视为失密；轮换使旧值立即失效，代码修复则阻止新密码再次随连接 URL 出现在错误堆栈。两者分别处理已经发生的暴露和未来的复发风险。
- 结果：凭据阻塞解除；下一步继续 JSON-only reliability graph 的 interrupt RED，发布门禁保持 `CLOSED`。

## 11:55 Asia/Shanghai｜JSON-only reliability graph RED/GREEN

- RED：新增真实 PostgreSQL/checkpointer 图测试后，收集阶段因 `opercerta.workflow` 不存在退出 1，准确证明图实现缺失。
- GREEN：实现 `prepare_approval → request_approval(interrupt) → approved/rejected` 最小拓扑；Repository 通过闭包注入，checkpoint state 只含 JSON-compatible 字段。
- 数据库断言：interrupt 时 operation 为 `awaiting_approval`、审批数 0、工单数 0；批准路径一条审批、一条工单和一个完成事件；拒绝路径一条审批、零工单和一个拒绝事件。
- 证据：focused `3 passed`、workflow 集成 `7 passed`，Ruff lint、全量 format check 与 mypy（18 个源文件）通过；提交 `2e6cbb4`。
- 下一步：编写 RecoveryCoordinator 与四点 A/B 重启矩阵 RED，发布门禁保持 `CLOSED`。

## 12:24 Asia/Shanghai｜RecoveryCoordinator 与四点重启矩阵完成

- RED：完整重启矩阵测试在收集阶段因 `recovery_coordinator` 模块缺失退出 1；测试先于协调器生产实现。
- GREEN：协调器读取业务恢复视图、分类 missing/interrupted/runnable checkpoint、调用现有确定性恢复矩阵，并只执行对应 rebuild/wait/resume/continue/no-op 动作。
- 场景：首 checkpoint 前重建、等待审批保持、批准后恢复、拒绝后恢复、工单预写安全重放均使用关闭的 saver A/graph A 和新 saver B/graph B；另覆盖 checkpoint operation ID 冲突、终态 pending checkpoint 和已关闭 checkpointer 基础设施异常。
- 数据库事实：等待路径零审批/零工单；拒绝路径零工单；批准路径一条审批/一条工单；预写路径返回原工单 ID、`replayed=True` 且只有一条创建审计；基础设施异常保持 `resuming` 和原审批。
- 重复验证：十个独立 Pytest 进程全部退出 0，每轮 `8 passed`，实测 `IndependentRestartRuns=10/10`；不解释为生产成功率。
- 完整门禁：`116 passed in 9.96s`，Ruff lint 通过，32 个文件 format check 通过，mypy 检查 19 个源文件通过，`git diff --check` 通过。
- 提交与证据：实现提交 `e93b551`；证据写入 `docs/release-evidence/langgraph-restart-recovery.md`。Task 5 完成不打开发布门禁；下一步为可靠性内核 Task 6。

## 12:34 Asia/Shanghai｜可靠性内核 Task 6 总门禁

- 新鲜门禁：`uv sync --frozen --all-groups` 成功；完整测试 `116 passed in 8.76s`；Ruff lint、32 文件 format check、mypy（19 个源文件）通过。
- 迁移诊断：首次直接 Alembic downgrade 因独立进程没有 `OPERCERTA_DATABASE_URL` 而在连接前退出 1，没有修改数据库或展开密码；根因是命令环境未复用测试 fixture，不是迁移脚本错误。
- 迁移闭环：复用 SecretStr、无密码 URL 和临时 `PGPASSWORD` 后，downgrade base → upgrade head → current 全部成功，当前版本 `0001_reliability_kernel (head)`；迁移后集成测试 `39 passed in 8.74s`。
- 证据：总门禁、已验证不变量、未验证范围和下一边界写入 `docs/release-evidence/reliability-kernel.md`；可靠性内核在当前范围冻结。
- 下一步：仍只实施 OperCerta，建立 event → evidence → plan → approval → simulated MCP write → verification → audit → API response 最小纵向闭环。发布门禁保持 `CLOSED`，不启动其他项目。

## 15:44 Asia/Shanghai｜库存补货纵向闭环设计确认

- 场景选择：首个业务闭环为“库存不足 → 补货工单”；首版总业务范围仍为库存查询、设备查询、补货工单和维修工单四个场景，故障恢复是横跨业务的技术路径，不额外计作业务场景。
- 业务规则：`available = on_hand - reserved`；`available < reorder_point` 时建议量为 `target_stock - available`；正常库存直接完成，零审批、零工单。
- 安全规则：所有补货写入强制审批；证据缺失、超时、错误或过期时安全关闭；审批绑定证据快照，批准后写入前重新取证，事实变化不得复用旧批准。
- 方案：采用独立 FastMCP 服务、四个真实 MCP 工具、Mock 结构化模型、LangGraph 和 FastAPI 的后端纵向切片；不使用进程内函数冒充 MCP，也不在本轮同时加入 React、JWT、SSE 和真实模型。
- 设计落盘：规格写入 `docs/superpowers/specs/2026-07-16-inventory-replenishment-vertical-slice-design.md`，覆盖组件职责、MCP 契约、数据库迁移、状态机、API、稳定错误、重启恢复和 TDD 验收矩阵。
- 当前事实：本次只写设计与日志，没有新增生产代码、迁移或测试结果。发布门禁保持 `CLOSED`；下一步为用户复核书面规格后编写聚焦实施计划。

## 16:20 Asia/Shanghai｜库存补货纵向闭环聚焦 TDD 计划完成

- 目标：把已确认的库存补货设计转换为逐文件、逐 RED/GREEN、可原子提交且无需实施时临场补设计的执行计划。
- 依赖核验：对照官方 PyPI 与本地锁定环境复核 MCP Python SDK `1.28.1`、FastAPI `0.139.0`、HTTPX `0.28.1`、LangGraph `1.2.9`、`langgraph-checkpoint-postgres 3.1.0`、Pydantic `2.13.4`、SQLAlchemy `2.0.51`、Alembic `1.18.5`；当前锁定版本适用，不在本切片升级。
- API 核验：实际检查 FastMCP Streamable HTTP 服务与工具装饰器、MCP `ClientSession`、`streamable_http_client`、`ToolError`、结构化工具结果以及 HTTPX `ASGITransport` 签名；计划不依赖猜测的接口。
- 计划结构：九个 Task 依次覆盖严格领域输入与确定性规则、`0002` 迁移和证据/operation Repository、绑定审批和过期竞态、真实 FastMCP 四工具、类型化 MCP gateway、审批前 LangGraph、审批后写入/回读/A-B 重启、FastAPI、最终门禁与证据。
- 自审修正：正常库存不调用 Mock 模型；证据记录与计划验证拆为独立审计迁移；新鲜证据 ID 与原审批 provenance 分离；审批后只比较规则版本、事实哈希、计划哈希和数量；补齐 `McpSessionFactory`、`AppRuntime`、Runner、Catalog ID factory 和真实 uvicorn fixture 接口；移除生产接口代码中的省略号占位。
- 自审证据：计划含 9 个 Task、53 个未执行检查框；Markdown code fence 数为偶数。占位/TODO 扫描无匹配。本步骤没有编写生产代码、迁移数据库或产生新通过指标。
- 结果：计划保存为 `docs/superpowers/plans/2026-07-16-inventory-replenishment-vertical-slice.md`，可从 Task 1 的领域非法输入 RED 开始。
- 边界：React、SSE、JWT/RBAC、真实模型、Docker/Linux 和公开部署仍不在本切片；`OperCerta release gate: CLOSED`，不启动其他项目。

## 库存补货 Task 1–2 与执行模式调整

- Task 1：领域契约首次 RED 因新错误/模块缺失而退出 1；实现后 focused `17 passed`。独立评审发现 Pydantic 宽松整数转换问题，新增 14 个字段 × 3 类非法类型回归，先得到 `42 failed`，修复为 `StrictInt` 后 focused `59 passed`、完整 unit `136 passed`；提交 `4a04113` 与 `b87b609`。
- Task 2：迁移 RED 明确找不到 revision `0002_inventory_replenishment`，Repository RED 因模块缺失；完成 `0002`、evidence Repository 和 operation 生命周期 Repository 后，评审回路补齐精确候选 payload、类型化详情、`FOR SHARE` 一致读取、终态事实校验、过期重放、locator 绑定、重复 evidence ID 和精确迁移回滚门禁；提交 `c035a46`、`c6d9f92`、`ef2ec15`。
- 诊断事件：一次实现代理与其 reviewer 并行运行共享 migration 测试，交叉 downgrade 导致瞬时 schema 缺失。停止并行数据库命令后，所有数据库门禁均由唯一进程串行复验；该失败未被写成通过。
- 新鲜复验：Task 2 focused `40 passed in 7.64s`；完整数据库集成 `64 passed in 11.85s`；Ruff lint 通过、43 个文件 format check 通过、mypy 检查 23 个源文件通过。
- 执行模式：用户观察逐 Task 的实现代理、评审代理和多轮修复代理耗时过长，明确从 Subagent-Driven Development 切换为 Inline Execution。后续由当前对话直接执行 TDD、系统化调试、自审和原子提交，只在纵向闭环完成前安排一次集中代码审查。
- 边界不变：切换执行方式不降低测试、事务、迁移、重启、凭据和发布门禁要求；`OperCerta release gate: CLOSED`。

## 库存补货 Task 3｜绑定审批、持久化过期与竞态

- RED：先新增绑定审批 PostgreSQL 集成测试与过期服务单元测试；首次运行在收集阶段因 `BoundApprovalCommand` 和 `opercerta.application.approval_expiry` 缺失产生 2 个明确错误，证明测试先于生产实现。
- 绑定审批：新增携带 `expected_binding` 的命令；Repository 在同一 operation 行锁事务内核对六个绑定字段、写入审批 provenance、把状态切换为 `resuming` 并追加 `approval_recorded`。旧 `submit_once()` 保持独立，继续允许历史 nullable binding。
- 过期语义：当 `now >= approval_expires_at` 时不插入审批，而是在同一事务内写入 `expired` 和唯一 `approval_expired` 审计；事务提交完成后再抛出稳定错误 `approval_expired`，防止异常回滚过期事实。
- 调试记录：首轮 GREEN 为 `12 passed, 1 failed`；唯一失败来自 PostgreSQL 按会话时区 `+08:00` 返回与 UTC 表示不同。根因确认是“同一时刻的字符串表示不同”，测试改为对照数据库持久化的 expiry 事实，未修改业务过期边界；随后 focused `13 passed`。
- 兼容与竞态：旧审批竞态和重启恢复回归 `12 passed`；十路绑定审批竞态在 10 个独立 pytest 进程中全部完成，每轮断言恰好一个成功决定、九个 `approval_already_decided`，记录为 `completed_processes=10`，不解释为生产成功率。
- 新鲜门禁：完整数据库集成 `75 passed in 15.45s`；Ruff lint 通过；44 个文件 format check 通过；mypy 检查 25 个源文件通过；`git diff --cached --check` 通过。
- 提交与边界：代码原子提交为 `d97c5bb feat: bind approvals to evidence`。没有修改迁移、启动 MCP/Workflow、接触其他项目或填写虚构指标；`OperCerta release gate: CLOSED`。下一步按计划实施 Task 4 的版本化合成目录与真实 FastMCP 工具服务。
