# 2026-07-17 开发日志

## 本次目标

为已确认的 Docker/Linux 运行时设计创建可执行的测试驱动实施计划；范围仍只限 OperCerta。

## 已完成

- 已确认当前 Windows 10 Enterprise LTSC 2021 build 19044.5011 不适合安装当前官方 Docker Desktop，运行时路线保持为 Hyper-V、Ubuntu 24.04 LTS VM 与官方 Docker Engine/Compose。
- 已将 API/MCP 健康契约、bootstrap 边界、非 root Compose、秘密处理、真实业务 smoke test 与重启恢复拆分为 0–4 五个可独立复核的任务。
- 计划保留 Release Gate 为 CLOSED，明确排除 Redis、前端、认证、公开部署和其他项目。
- Task 1 已按 RED/GREEN 实施 API `/health/live` 与 `/health/ready`：liveness 不触发依赖探针；readiness 的异常路径固定返回不含异常详情的 `503`。提交为 `143be69`。
- Task 2 已实现生产 readiness 探针、MCP 健康包装层和 API/MCP/bootstrap 容器入口。实现时核验 AsyncEngine 必须使用 `async with` 与 `await execute`；FastMCP 包装层需要显式承接其 lifespan，且测试 Host 必须包含受信任 loopback 端口。上述均以自动化测试固定，未宣称 Docker/Linux 已验证。
- Task 2 全量本机门禁：`290 passed in 57.75s`，Ruff、格式检查和 `mypy src` 均通过；这些是 Windows 原生 PostgreSQL 验证，不是 Linux/Docker 证据。
- 用户已确认将运行环境从 Hyper-V Ubuntu 24.04 VM 修订为 WSL2 Ubuntu 26.04；Windows PostgreSQL 保留作本机测试库，Compose PostgreSQL 保持独立。
- WSL2 已实际运行 Ubuntu 26.04 LTS（`resolute`）。原计划的 Docker 厂商 APT 源在 Windows 与 WSL2 中均无法完成 `download.docker.com` TLS 握手；DNS 正常、Ubuntu 官方 HTTPS 对照请求成功，故定位为当前网络路径/安全策略对 Docker 站点的阻断，而非 WSL2、证书或安装命令问题。
- 经用户确认，改用 Ubuntu 官方签名仓库安装 `docker.io 29.1.3-0ubuntu4.1`、`docker-compose-v2 2.40.3+ds1-0ubuntu1` 和 `docker-buildx 0.30.1-0ubuntu1`。Docker Engine 客户端与服务端均为 `29.1.3`，`docker.service` 为 `active`；没有改动 Windows PostgreSQL、项目仓库或密钥文件。
- `docker run --rm hello-world` 未通过：Docker Hub `registry-1.docker.io:443` 超时。该事实阻断 Python、PostgreSQL、uv 等 OperCerta 基础镜像拉取；尚未产生镜像 digest、Compose 启动或 Linux 业务闭环证据，发布门禁保持关闭。
- 用户随后提供并授权配置第三方公共镜像代理。WSL2 只读探针显示 DaoCloud、1ms、轩辕三个地址均以 `401` 正确响应 Registry v2，`dockerproxy.com` 在 10 秒内超时；因此仅把前三项写入新建的 `/etc/docker/daemon.json`，没有覆盖旧配置，也没有记录账户或凭据。
- 重启 Docker 后，`docker info` 已显示三个 registry mirror。普通名称 `hello-world` 成功拉取、运行，digest 为 `sha256:c3cbe1cc1aa588a64951ac6286e0df7b27fe2e6324b1001c619bb358770c0178`。这仅验证 Docker 的拉取和执行基础链路；公共代理是第三方供应链例外，OperCerta 基础镜像和 Compose 仍须以实际 digest、健康检查、数据库断言和业务 smoke test 验收。
- OperCerta Compose 首次真实构建暴露 Dockerfile 缺少 Hatchling 所需 `README.md`/源码输入；PostgreSQL 18 首次启动暴露旧 volume 挂载点；首次业务 smoke 暴露 FastMCP 拒绝 `Host: mcp:8001`。三项均先以 RED 测试固定后最小修复，没有删除 named volume。
- 最终 Compose 构建、四服务健康等待、真实低库存审批闭环与数据库断言均通过；API/MCP 重启后 `--recovery-only` 再次通过。最终本机质量门禁为 `297 passed in 62.67s`，Ruff、79 文件格式检查和 mypy（41 个源文件）均通过。完整命令、版本、镜像标识、回滚方式和未验证范围见 `docs/release-evidence/docker-linux-runtime.md`；实现提交为 `91cffa8`。
- 已创建 `docs/development-log/interview-casebook.md`，把真实的审批竞态、恢复边界、事件循环、凭据轮换、Docker 构建、PostgreSQL 18 volume、MCP Host 和镜像供应链问题沉淀为面试复盘案例；每项明确已验证事实与未验证限制。
- 已创建 `docs/development-log/learning-method.md`，固定“读 20% → 动手 60% → 讲解 20%”、单变量实验规则、五阶段学习路线和每周复盘模板；后续实现、故障和术语问题应持续补充。

## 下一步

进入发布门禁剩余范围，优先认证/人工接管、固定评测、安全回归和可观测性；不启动其他项目。
