# 2026-07-15 开发日志

## 17:43 Asia/Shanghai｜建立日志与上下文恢复机制

- 目标：把关键工程事实从对话上下文转为可提交、可复查的仓库记录。
- 观察：当前分支为 `main`；`git remote -v` 无输出；可靠性内核的纯 Python 单元测试已通过。
- 动作：确认开发日志采用当前状态、每日过程、决策记录三层结构，并增加根目录 `DOCUMENT_INDEX.md` 作为文档目录。
- 证据：日志设计提交 `7e3b353`；文档索引设计提交 `a0564b1`；日志初始化计划提交 `6c97d5d`；`uv run pytest tests/unit -q` 输出 `19 passed`，退出码 0。
- 结果：日志文件已初始化，根目录文档索引已创建并指向实际记录。
- 风险与下一步：日志不替代测试证据；下一步创建根目录文档索引，然后执行 Windows 原生 PostgreSQL 环境计划。

## 17:48 Asia/Shanghai｜日志初始化完成并移交 PostgreSQL 环境计划

- 目标：确认文档索引与三层日志可作为后续上下文恢复入口。
- 观察：根目录 `DOCUMENT_INDEX.md` 已列出当前设计、计划、日志和计划创建的证据目录。
- 动作：完成索引路径校验、Git 提交和当前状态更新。
- 证据：索引路径校验通过；敏感值扫描无匹配；文档索引提交 `b29e2a2`。
- 结果：日志机制已可用；业务实现尚未开始新的数据库代码。
- 风险与下一步：继续执行 Windows 原生 PostgreSQL 环境计划 Task 1；不因日志完成改变发布门禁。

## 17:52 Asia/Shanghai｜Windows 原生 PostgreSQL 环境基线

- 目标：确认可靠性内核 Task 3 的本机数据库前置条件尚未满足，避免重复安装或误连其他服务。
- 观察：`git status --short --branch` 为干净的 `main`；`.env.local` 命中 `.gitignore` 的 `.env.*` 规则；未找到 `psql.exe`，PostgreSQL 服务数量为 0，`127.0.0.1:55432` 连接失败。
- 动作：执行 PostgreSQL 基线探针，未写入业务代码、数据库或凭据。
- 证据：PowerShell 基线对象为 `PsqlFound=False`、`PostgreSQLServices=0`、`Port55432Open=False`；命令退出码 0。
- 结果：可以从官方渠道获取 PostgreSQL 18.4 Windows x86-64 安装器。
- 风险与下一步：安装器下载和密码输入必须在本地可见界面完成；下载后先核验数字签名和 SHA-256，再请求安装权限。

## 21:44 Asia/Shanghai｜Windows 原生 PostgreSQL 环境验证

- 目标：为审批竞态和幂等写入提供只绑定本机回环的真实 PostgreSQL。
- 观察：安装前无 `psql.exe`、无 PostgreSQL 服务、`127.0.0.1:55432` 未监听；Windows 下载目录映射为 `D:\Downloads`。
- 动作：核验官方 EDB 安装器签名，交互式安装 PostgreSQL 18.4，设置端口 `55432`，收紧监听为 `127.0.0.1`，启用 SCRAM，创建本地测试角色与数据库，并以已忽略 `.env.local` 完成连接探针。
- 证据：安装器签名为 `Valid`、签名主体为 EnterpriseDB Corporation、SHA-256 已记录于 `docs/release-evidence/native-postgres-environment.md`；服务为 `Running/Automatic`；HBA 规则显示 IPv4 `scram-sha-256` 和 IPv6 `reject`；SQLAlchemy 探针连接 `opercerta_test`/`opercerta` 成功。
- 结果：本机 Task 3 PostgreSQL 前置条件已满足；发布门禁仍为 `CLOSED`。
- 风险与下一步：迁移、审批竞态、幂等写入和重启恢复尚未实现；下一步按 TDD 创建审批竞态 RED 测试。

## 21:49 Asia/Shanghai｜类型检查覆盖修复

- 目标：确认默认 mypy 命令实际检查 OperCerta 源代码，而不是仅以退出码 0 结束。
- 观察：默认 `uv run mypy` 提示缺少 `py.typed`；直接执行 `uv run mypy src\\opercerta` 已证明 5 个源文件无类型错误。
- 动作：添加 PEP 561 标记文件 `src/opercerta/py.typed`。
- 证据：默认 `uv run mypy` 输出 `Success: no issues found in 5 source files`；`uv run pytest -q` 为 `19 passed`；`uv run ruff check .` 通过；提交 `84a7b08`。
- 结果：默认类型检查现在是有效门禁。
- 风险与下一步：继续以 TDD 创建 PostgreSQL 迁移与审批竞态 RED 测试；不因静态检查通过改变发布门禁。

## 22:11 Asia/Shanghai｜补齐审批领域契约设计

- 目标：在 Task 3 写测试与生产代码前，补齐实施计划引用但尚未定义的审批 Python 契约。
- 观察：现有代码只有操作请求和恢复策略；实施计划引用 `ApprovalCommand`、`ApprovalRecord`、`ApprovalDecision`、`ApprovalAlreadyDecided` 和 `OperationNotFound`，但没有定义其字段和错误边界。
- 动作：核对详细设计状态机并形成独立审批契约设计补充；确认批准与拒绝均应先原子进入 `resuming`，再由恢复节点路由，而不是在 Repository 中直接产生执行或拒绝终态。
- 证据：`docs/specs/2026-07-14-opercerta-design.md` 第 81–95 行定义 `AwaitingApproval -> Resuming` 及其崩溃恢复理由；新增 `docs/superpowers/specs/2026-07-15-approval-domain-contract-design.md`。
- 结果：领域接口、稳定错误代码、事务后置条件和 TDD 验收边界已有可复核书面定义；尚未写审批生产代码。
- 风险与下一步：书面设计经用户复核后更新 Task 3 实施计划，再从领域契约 RED 测试进入 PostgreSQL 审批竞态；发布门禁保持 `CLOSED`。

## 22:24 Asia/Shanghai｜审批契约设计确认并回填实施计划

- 目标：把已确认的审批字段、错误语义和 TDD 顺序转为可以直接执行的 Task 3 步骤。
- 观察：原 Task 3 已定义数据库行锁和唯一约束，但没有创建 `ApprovalDecision`、`ApprovalCommand`、`ApprovalRecord`、`OperationNotFound` 与 `ApprovalAlreadyDecided` 的任务。
- 动作：在可靠性内核计划中增加领域契约单元测试 RED、最小 Pydantic/错误实现 GREEN、精确命令和独立提交步骤；保留已验证的 PostgreSQL 前置条件。
- 证据：书面设计提交 `3c55f3b`；计划自检确认字段名称、错误代码、`resuming` 后置状态和后续 Repository 使用方式一致。
- 结果：Task 3 现在可从缺失模块导致的预期 RED 开始，不需要在实现时猜测领域接口。
- 风险与下一步：审批模型、迁移和 Repository 仍未实现；下一步创建 `tests/unit/domain/test_approvals.py` 并实际观察 RED，发布门禁保持 `CLOSED`。

## 22:35 Asia/Shanghai｜审批领域契约完成首个 RED/GREEN

- 目标：实现 Task 3 所需的不可变审批命令、审批记录、决定枚举和稳定领域错误。
- 观察：新增测试首次运行因 `opercerta.domain.approvals` 不存在而在收集阶段失败，退出码 1；这是计划要求的缺失契约 RED。
- 动作：最小实现 `ApprovalDecision`、`ApprovalCommand`、`ApprovalRecord`、`OperationNotFound` 和 `ApprovalAlreadyDecided`；Ruff 首轮发现 import block 后多余空行，核对 `--diff` 后只删除两行空白。
- 证据：目标测试 `10 passed`；完整单元测试 `29 passed`；Ruff 通过；默认 mypy 输出 `Success: no issues found in 6 source files`；实现提交 `b87ef7f`。
- 结果：审批领域接口已由测试锁定，后续 Repository 不再依赖未定义类型。
- 风险与下一步：数据库表和原子事务尚未实现；已将迁移步骤修正为先写 Schema 契约 RED，再创建 Alembic 迁移。发布门禁保持 `CLOSED`。

## 22:59 Asia/Shanghai｜Task 3 迁移与原子审批竞态完成

- 目标：用真实 PostgreSQL 证明同一 operation 的并发审批只接受一个决定，并把审批、状态和审计放入同一事务。
- 观察：迁移测试先因 Alembic 配置缺失 RED；竞态测试先因 infrastructure 模块缺失 RED。Windows 首轮异步连接又暴露 Psycopg 不支持 `ProactorEventLoop`，改用 Selector loop 后恢复。
- 动作：创建四张业务表和独立 `langgraph` Schema；实现 SQLAlchemy 表映射与 `SELECT ... FOR UPDATE` 审批 Repository；为 Windows fixture 增加 UTF-8 BOM 兼容、`SecretStr`、无密码 URL 和临时 `PGPASSWORD`。
- 证据：迁移提交 `85e6538`；原子审批提交 `b37a659`；数据库集成 `5 passed`，完整测试 `34 passed`，Ruff 通过，mypy 检查 10 个源文件通过；十路竞态重复 `20/20` 通过。
- 结果：Task 3 数据库迁移和审批竞态已完成；批准与拒绝均只原子记录一次并进入 `resuming`，不存在或非等待状态零写入。
- 安全事件：一次失败 traceback 展开了本地测试密码；未把该值写入仓库或日志，测试输出路径已脱敏。继续 Task 4 前必须由用户在本机不回显地轮换角色密码并更新已忽略 `.env.local`。
- 风险与下一步：幂等工单与 LangGraph 重启恢复未实现，发布门禁保持 `CLOSED`；先完成密码轮换和连接复验，再开始 Task 4 RED。
