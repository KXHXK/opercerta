# 2026-07-18 开发日志

## 前端单页控制台

- 按既有单页控制台规格完成 Task 1 至 Task 4：Vite/React 工程、内存 JWT 会话、类型化 REST client、fetch SSE 快照解析与运营控制台组合。
- TDD 记录：SSE 解析器先因模块缺失 RED，后通过；重连测试明确“最多重连三次”表示初始连接后最多三次重新连接，即最多四次请求；审批、控制区、详情区、边界区和 App 组合测试均先观察失败再实现。
- 发现并修正测试隔离配置：Vitest 未提供全局 `afterEach`，React Testing Library 未自动清理 DOM。根因是 `test-setup.ts` 仅加载断言扩展；显式注册 `afterEach(cleanup)` 后，测试之间不再共享 DOM。
- 发现 `@testing-library/user-event` 不在依赖中。没有盲目新增依赖，确认唯一引用点后改用已安装 Testing Library 的 `fireEvent`，保持测试范围最小。
- 前端完整门禁：`npm run test:run` 为 9 个测试文件、15 个测试通过；`npm run build` 通过。
- 同轮后端回归：`uv run pytest -q` 为 `325 passed in 91.25s`；Ruff clean；91 个 Python 文件格式正确；mypy 检查 46 个源文件无问题。

## 边界与后续

- 本轮只实施 OperCerta 的本地演示前端；没有接入生产 IAM/SSO、实时订阅、公开部署或其他项目。
- 新增证据 `docs/release-evidence/single-page-console.md`，明确前端测试的 mock 边界，避免把客户端模拟误称为完整后端端到端验证。
- 发布门禁继续保持 `CLOSED`；同轮后端回归已记录为 `325 passed`，Ruff、format 和 mypy 通过。

## 可观测性与安全回归设计

- 用户确认下一阶段先做不需要外部账号授权的可观测性与安全回归基础，再处理 CI/CD、Caddy/HTTPS 和公开部署。
- 选择 Python 标准日志、服务端 `request_id` 与应用级 Prometheus registry；不在本阶段加入 OpenTelemetry、Grafana、Redis 业务依赖或生产 IAM。
- 官方 PyPI 元数据核验：`prometheus-client==0.25.0` 仍为当前版本；FastAPI 当前为 `0.139.2`，仓库锁定 `0.139.0`。规格要求把补丁升级拆成独立门禁，失败时恢复旧版本，不与业务改动混写。
- 规格已写入 `docs/superpowers/specs/2026-07-18-observability-security-regression-design.md`；发布门禁保持 `CLOSED`。
- 已把确认规格拆为七项原子 TDD 任务，计划见 `docs/superpowers/plans/2026-07-18-observability-security-regression.md`；本步骤只创建计划，尚未升级依赖或修改生产代码。

## 可观测性与安全回归实施

- Task 1 将 FastAPI 从 `0.139.0` 独立升级到官方当前补丁版 `0.139.2`；升级前版本断言 RED，升级后版本断言与 `325 passed` 完整回归通过，提交 `35f0b59`。
- Task 2–4 按 RED/GREEN 分别实现请求 `ContextVar`、安全 JSON 白名单日志和应用级独立 Prometheus registry；提交为 `82e2bcd`、`3eefc17`、`132b398`。
- Task 5 实现纯 ASGI middleware：服务端生成 request_id、忽略恶意客户端 header、稳定 503、响应头关联、完成时日志与指标、异常后上下文清理；`/metrics` 默认关闭，显式启用才注册。相关 API 回归 `15 passed`，提交 `e73829b`。
- Task 6 的 SSE 测试先证明事件 2 已实际发送但指标无样本；只在实际 yield 前计数后，`Last-Event-ID` 跳过的事件不计数，SSE/API 回归 `14 passed`，提交 `97ae5a3`。
- 合成 JWT 测试最初使用不足 32 字节的 HMAC key，PyJWT 给出安全长度警告。根因是测试夹具本身而非生产配置；把合成 key 调整为 32 字节以上后警告消失，没有把警告静默过滤。
- 全量静态门禁首次发现 3 个本轮文件需要 Ruff 格式化。先使用 `ruff format --check --diff` 确认只有换行变化，再用项目 formatter 修正并单独提交 `b9b793c`，避免与证据文档混写。
- 敏感文本扫描首次命中固定评测执行器的两处 Authorization 合成器。代码与 `git blame` 证明它们是既有 tampered/wrong-issuer 攻击样本，不是凭据；没有为了让扫描变绿而拆字或删除测试。显式排除该已审计文件后，其余交接、证据和运行源码零匹配。

## 本轮完整证据与边界

- `uv run pytest -q`：`332 passed in 74.58s`。
- 3 个文件纯格式化后的最终复验：`332 passed in 74.70s`；该耗时差异不解释为性能变化。
- `uv run ruff check .`：`All checks passed!`。
- `uv run ruff format --check .`：`100 files already formatted`。
- `uv run mypy src`：`Success: no issues found in 50 source files`。
- `web/` 下 `npm run test:run`：9 个测试文件、15 条测试通过；`npm run build` 成功。
- 未实现 OpenTelemetry、Grafana、集中采集/告警、生产 IAM、CI/CD、Caddy/HTTPS 或公开部署；发布门禁仍为 `CLOSED`。

## GitHub Actions CI 安全门禁设计

- 用户确认使用 GitHub Actions，并选择仓库初始为 Private；转 Public 必须在发布门禁后再次审批。
- 选择分层方案：PR/push 运行仓库安全、Python 静态门禁、PostgreSQL 18 完整后端测试和前端测试/构建；`main` 或手动触发追加 Compose 真实业务 smoke 与重启恢复。
- 官方 release API 核验当前 action tag：checkout `v7.0.0`、setup-python `v6.3.0`、setup-node `v7.0.0`、setup-uv `v8.3.2`。实际 YAML 将重新解析并固定完整 commit SHA，不直接使用 tag。
- 现有数据库 fixture 已核实位于 `tests/integration/conftest.py`，会执行 Alembic upgrade 与 checkpointer setup；最初假设根级 `tests/conftest.py` 的路径错误，没有据此修改代码。
- CI 安全扫描会精确 allowlist 固定评测执行器的两处攻击 token 样本，未知路径、模式或数量仍失败；不通过拆字规避扫描。
- 本步骤只完成并确认设计；尚未创建 GitHub remote、workflow、CI run 或分支保护，也没有部署。
- 已把确认设计拆为六项可执行任务：仓库安全扫描器、四个快速 Actions job、main Compose smoke、本地总门禁、Private remote 与真实 PR/main run、主分支保护和证据交接。计划见 `docs/superpowers/plans/2026-07-18-github-actions-ci-security-gate.md`；本步骤仍未创建远程或修改运行代码。

## GitHub Actions CI 安全门禁实施

- 在隔离 worktree 的 `ci/github-actions-gate` 分支按 TDD 实施。Task 1 新增受测试保护的仓库安全扫描器；RED 首先证明模块缺失，GREEN 后锁定受禁环境文件、凭据模式、精确攻击样本 allowlist、Action 完整 SHA 和只读权限。
- 扫描器首次真实运行把演示证据中的普通描述误识别为凭据，新增回归测试后把 Bearer 检测收紧为至少 16 个字符；没有删除攻击测试或拆字逃避扫描。
- Task 2 新增四个快速 job：`repository-safety`、`python-quality`、PostgreSQL 18 `backend-tests`、`frontend`。workflow 解析、6 条契约测试、安全扫描、Ruff 和格式检查通过，提交 `61d7822`。
- Task 3 先写缺少 `compose-smoke` 的 RED，再实现仅 `main` push/手动触发的构建、库存补货 smoke、API/MCP 重启、恢复验证、失败状态诊断和无条件卷清理。完整本地门禁为后端 `339 passed in 77.34s`、mypy 50 个源文件、前端 9 文件/15 条测试和生产构建，提交 `265d611`。
- 用户确认 GitHub owner 为 `KXHXK` 并手工创建 Private `KXHXK/opercerta`。本地通过 Git Credential Manager 保存账号并正常推送 `main` 与功能分支，无 force push。
- GitHub 插件只完成了用户身份核验，但安装列表为空，无法读取新 Private 仓库；内置浏览器两次加载比较页超时。为避免依赖浏览器自动化，下载 GitHub 官方便携 CLI `2.96.0`，校验 SHA-256 后放入用户本地工具目录，不修改系统 PATH。
- GitHub CLI 设备登录接口在当前网络超时；复用 Git Credential Manager 凭据做持久登录时，又因缺少 CLI 强制要求的 `read:org` scope 被拒绝。最终采用最小权限方式：凭据只进入单个子进程的 `GH_TOKEN` 环境变量，命令结束立即清除；未在日志、文档或项目文件中输出 token。
- PR `#1` 创建并合并。PR run `29642286517` 四个快速 job 成功，`compose-smoke` 按设计跳过；main run `29642363033` 五个 job 全部成功。
- main 远程日志实测：Ruff 通过、104 个文件格式正确、mypy 50 个源文件、后端 `339 passed in 29.46s`、前端 9 文件/15 条测试、0 个 npm 审计漏洞、Compose 业务 smoke、API/MCP 重启恢复和无条件资源清理均成功。
- setup-uv 并发保存相同 cache 出现非致命 reservation annotation，但相关 job 与 run 结论均为 `success`；没有加入 `continue-on-error`。
- 读取 Private `main` branch protection 返回 HTTP 403，GitHub 要求升级账户计划或把仓库改为 Public。按已确认边界不公开、不购买、不重试写入，明确记录保护未启用，并采用人工 PR 四门禁全绿后合并、main Compose 再验证的替代规则。
- 完整远程证据见 `docs/release-evidence/github-actions-ci.md`。这完成了分层 CI，不代表生产 IAM、HTTPS、自动部署或公开上线；发布门禁继续为 `CLOSED`。
- 证据文档完成后执行最终本地总门禁：安全扫描通过；后端 `339 passed in 75.85s`；Ruff 通过；104 个文件格式正确；mypy 检查 50 个源文件无问题；前端 9 个测试文件/15 条测试通过，Vite 生产构建用时 2.40s。
- 证据 PR `#2` 的四个快速 job 通过并合并；合并 commit `4b392ed4eeb6998d4f2bd666880453c3bd4a275b` 对应 main run `29642949588` 的五个 job 再次全部成功。实施计划最后一步只在该事后证据产生后标记完成。
- 官方 tag 对应 SHA 已通过 `git ls-remote` 核验并写入计划；workflow 将固定完整 SHA，不使用可变 tag。

## 公开作品集专题实施

- 在隔离分支 `feature/public-portfolio-showcase` 实施。新 worktree 不携带被忽略的 `.env.local`，首次完整后端基线因缺少 `OPERCERTA_DATABASE_URL` 出现 122 个配置错误；以不回显、不复制凭据的临时环境变量加载后，分组验证单元 191 条、API/数据库 91 条、评估/MCP/工作流 57 条全部通过，共 339 条。
- Task 1 将根路径与本地控制台隔离：`/` 为静态专题，`/console` 仅在 localhost 或显式 API 地址下运行；运行时边界测试先因模块缺失 RED，后为 3 条 GREEN。提交 `e742d91`。
- Task 2 的专题内容测试先因缺少业务闭环、架构和未完成区块 RED；实现事实型内容和响应式样式后，前端新鲜门禁为 11 个测试文件、21 条测试通过，生产构建成功。
- Netlify 配置测试先因 `netlify.toml` 缺失 RED，静态构建目录与 SPA rewrite 配置后 GREEN。当前只证明配置和本地构建，尚未创建或部署外部站点。
- 公开页面不调用 API，不提供可写服务，不链接无法访问的 Private GitHub；生产 IAM/SSO、设备场景、自动部署、完整浏览器 E2E 与真实模型评测仍未完成，发布门禁保持 `CLOSED`。

## 公开专题真实本地证据

- 浏览器首次能读取 operation 详情但审计回放失败；API 容器日志给出 `/events` 为 404。源码已有该路由，镜像创建时间却早于实现，根因是运行了旧镜像。重新构建当前源码后直接请求得到 `200 text/event-stream`。
- 多次 `docker compose up -d` 后全部容器在同一秒以 0 退出；PostgreSQL 明确记录管理员快速关机。确认不是应用异常，而是 WSL 无持久会话时实例和 Docker daemon 一起结束，且 `Live Restore` 关闭。本轮使用临时、可追踪的 `sleep infinity` 会话保活，未写系统启动项。
- 镜像首次重建的 `uv sync` 耗时较长但下载连接、磁盘写入持续增长；没有误判为死锁或并行启动重复构建。根因之一是 README 变化使当前 Dockerfile 的依赖层缓存失效，后续可单独优化镜像分层。
- 使用 `SKU-LOW-001` 完成 operation `670eac76-2c52-48ce-9091-ecb2c55a236f`：推荐数量 18、审批 `approved`、唯一工单 `8289d0d0-e1a5-41ed-aec9-a049390f6d15`、工单状态 `created`、operation `completed`、审计序列 10。
- 前端原本没有展示后端工单 ID。新增组件 RED 测试后补充工单编号和状态；随后截图 alt 文本测试先 RED，再把两张本地合成数据截图接入静态专题。
- 新鲜前端门禁：11 个测试文件、24 条测试通过，Vite 生产构建成功。此时外部 Netlify 站点尚未创建；后续部署过程记录见下一节，原发布门禁始终为 `CLOSED`。

## Netlify 静态专题生产部署

- 经用户明确授权，使用 Netlify CLI OAuth 登录并创建、连接 `opercerta-kxh`，生产地址为 <https://opercerta-kxh.netlify.app>。
- 当前网络下 `npx` 首次安装 Netlify CLI 出现空的嵌套依赖目录。只清理该次失败的临时 `_npx` 缓存，再以固定的兼容版本 `netlify-cli@26.2.0` 和 legacy peer dependency 模式安装成功；没有修改项目依赖。
- 第一次生产部署返回成功，但线上标题、JS/CSS 指纹和证据图片响应证明它发布了主 checkout 的旧 `web/dist`。成功退出码只能证明上传完成，不能证明上传内容正确。
- `netlify build --dry --debug` 定位到嵌套 Git worktree 与站点 UI 的 base 设置共同导致 repository root 回到主 checkout。使用功能工作树内已验证的 `web/dist` 执行显式 `--no-build --dir=web/dist` 后，最终 deploy id 为 `6a5ba2ebacf340a5a0649f91`。
- 独立 HTTPS 核验：`/`、`/console` 为 `200 text/html`；JS/CSS 指纹与本地构建一致；两张流程图为 `200 image/png`；`/api/v1/auth/demo-token` 为静态 HTML 回退，未公开后端。
- 静态专题已上线不改变原 OperCerta release gate：生产 IAM/SSO、Caddy/HTTPS 后端、自动部署、完整浏览器 E2E 和公开 API 仍未完成，门禁继续为 `CLOSED`。

## 个人作品集入口

- 按已批准 Task 5 检查 `D:\CODEX\resume\portfolio`：工作区在本轮之前已有大量未提交改动，`app/page.tsx` 为用户正在进行的整文件重写，全部继续视为用户所有。
- 未修改前执行 `npm run build`，Windows 因 `WRANGLER_LOG_PATH=...` 的 POSIX 写法无法启动；WSL 内又没有 Node。使用项目外的子进程级 `npm_config_script_shell=F:/Git/bin/bash.exe` 后，等价基线构建成功，证明不是页面代码失败。
- 最小契约先观察到 OperCerta 名称、真实 URL 和安全外链属性全部缺失的 RED；只追加第 04 张项目卡与可选外链后 GREEN。
- 使用原 build script 完成五阶段 Vinext 生产构建，并从生成的 SSR worker 获取首页 HTML，验证 OperCerta 外链包含真实 URL、`target="_blank"` 和 `rel="noreferrer"`。
- 因新增行与用户整文件重写处于同一 Git hunk，无法在不吸收用户改动的前提下单独暂存；本轮保持 `app/page.tsx` 未暂存、未提交，也未部署个人作品集。
