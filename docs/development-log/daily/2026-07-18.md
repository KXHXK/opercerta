# 2026-07-18 开发日志

## 前端单页控制台

- 按既有单页控制台规格完成 Task 1 至 Task 4：Vite/React 工程、内存 JWT 会话、类型化 REST client、fetch SSE 快照解析与运营控制台组合。
- TDD 记录：SSE 解析器先因模块缺失 RED，后通过；重连测试明确“最多重连三次”表示初始连接后最多三次重新连接，即最多四次请求；审批、控制区、详情区、边界区和 App 组合测试均先观察失败再实现。
- 发现并修正测试隔离配置：Vitest 未提供全局 `afterEach`，React Testing Library 未自动清理 DOM。根因是 `test-setup.ts` 仅加载断言扩展；显式注册 `afterEach(cleanup)` 后，测试之间不再共享 DOM。
- 发现 `@testing-library/user-event` 不在依赖中。没有盲目新增依赖，确认唯一引用点后改用已安装 Testing Library 的 `fireEvent`，保持测试范围最小。
- 前端完整门禁：`npm run test:run` 为 9 个测试文件、15 个测试通过；`npm run build` 通过。
- 同轮后端回归：`uv run pytest -q` 为 `325 passed in 91.25s`；Ruff clean；91 个 Python 文件格式正确；mypy 检查 46 个源文件无问题。

## 边界与后续

- 本轮只实施 OperCerta 的本地演示前端；没有接入生产 IAM/SSO、实时订阅、公开部署或其他项目。
- 新增证据 `docs/release-evidence/single-page-console.md`，明确前端测试的 mock 边界，避免把客户端模拟误称为完整后端端到端验证。
- 发布门禁继续保持 `CLOSED`；同轮后端回归已记录为 `325 passed`，Ruff、format 和 mypy 通过。

## 可观测性与安全回归设计

- 用户确认下一阶段先做不需要外部账号授权的可观测性与安全回归基础，再处理 CI/CD、Caddy/HTTPS 和公开部署。
- 选择 Python 标准日志、服务端 `request_id` 与应用级 Prometheus registry；不在本阶段加入 OpenTelemetry、Grafana、Redis 业务依赖或生产 IAM。
- 官方 PyPI 元数据核验：`prometheus-client==0.25.0` 仍为当前版本；FastAPI 当前为 `0.139.2`，仓库锁定 `0.139.0`。规格要求把补丁升级拆成独立门禁，失败时恢复旧版本，不与业务改动混写。
- 规格已写入 `docs/superpowers/specs/2026-07-18-observability-security-regression-design.md`；发布门禁保持 `CLOSED`。
- 已把确认规格拆为七项原子 TDD 任务，计划见 `docs/superpowers/plans/2026-07-18-observability-security-regression.md`；本步骤只创建计划，尚未升级依赖或修改生产代码。

## 可观测性与安全回归实施

- Task 1 将 FastAPI 从 `0.139.0` 独立升级到官方当前补丁版 `0.139.2`；升级前版本断言 RED，升级后版本断言与 `325 passed` 完整回归通过，提交 `35f0b59`。
- Task 2–4 按 RED/GREEN 分别实现请求 `ContextVar`、安全 JSON 白名单日志和应用级独立 Prometheus registry；提交为 `82e2bcd`、`3eefc17`、`132b398`。
- Task 5 实现纯 ASGI middleware：服务端生成 request_id、忽略恶意客户端 header、稳定 503、响应头关联、完成时日志与指标、异常后上下文清理；`/metrics` 默认关闭，显式启用才注册。相关 API 回归 `15 passed`，提交 `e73829b`。
- Task 6 的 SSE 测试先证明事件 2 已实际发送但指标无样本；只在实际 yield 前计数后，`Last-Event-ID` 跳过的事件不计数，SSE/API 回归 `14 passed`，提交 `97ae5a3`。
- 合成 JWT 测试最初使用不足 32 字节的 HMAC key，PyJWT 给出安全长度警告。根因是测试夹具本身而非生产配置；把合成 key 调整为 32 字节以上后警告消失，没有把警告静默过滤。
- 全量静态门禁首次发现 3 个本轮文件需要 Ruff 格式化。先使用 `ruff format --check --diff` 确认只有换行变化，再用项目 formatter 修正并单独提交 `b9b793c`，避免与证据文档混写。
- 敏感文本扫描首次命中固定评测执行器的两处 Authorization 合成器。代码与 `git blame` 证明它们是既有 tampered/wrong-issuer 攻击样本，不是凭据；没有为了让扫描变绿而拆字或删除测试。显式排除该已审计文件后，其余交接、证据和运行源码零匹配。

## 本轮完整证据与边界

- `uv run pytest -q`：`332 passed in 74.58s`。
- 3 个文件纯格式化后的最终复验：`332 passed in 74.70s`；该耗时差异不解释为性能变化。
- `uv run ruff check .`：`All checks passed!`。
- `uv run ruff format --check .`：`100 files already formatted`。
- `uv run mypy src`：`Success: no issues found in 50 source files`。
- `web/` 下 `npm run test:run`：9 个测试文件、15 条测试通过；`npm run build` 成功。
- 未实现 OpenTelemetry、Grafana、集中采集/告警、生产 IAM、CI/CD、Caddy/HTTPS 或公开部署；发布门禁仍为 `CLOSED`。
