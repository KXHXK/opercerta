# 2026-07-22 开发日志

## 目标

继续执行 OperCerta Agent 核心架构计划：完成 Task 5 审批后 Verifier，并实施 Task 6 的 pgvector、中文合成 SOP、FastEmbed、MCP RAG、LangGraph 引用与 Memory 边界。未启动 ForenTrail。

## 已完成

- Task 5 已提交为 `8794f33`：三业务批准后重取证、`proceed/abort/escalate`、多轮复审、恢复、竞态与幂等写入。
- Task 6 新增 `0005_agent_knowledge`、pgvector 512 维表与 HNSW 索引、并发幂等知识仓储、固定 FastEmbed gateway、MCP `knowledge.search_sop`、最小分数过滤和 LangGraph citation 注入。
- 三份从零编写的合成中文 SOP 已通过 manifest、SHA-256 和固定 H2 分块校验；正常入库才调用模型与数据库，`--check` 不下载模型、不连接数据库。
- API 默认开启可选 RAG、默认不把 SOP 设为强制证据；MCP 模型缓存使用非 root 可写命名卷。普通检索失败降级，规则明确要求 SOP 时失败关闭。
- 真实固定模型已完成首次入库、幂等重放和三场景代表检索；未虚构准确率或 token/成本。

## RED/GREEN 与问题复盘

1. 工作流接口 RED 证明旧图不能启用知识；实现后 citations 只包含当前场景文档。
2. 强制 SOP 缺失最初触发空计划 ValidationError；根因是失败工具也被视为已执行，而路由仍要求 replan。修复为知识必需且失败时立即安全终止并传播稳定错误码。
3. 正交向量得分 0 最初仍返回；补充固定最小分数 0.5 后返回 `knowledge_insufficient`。
4. 官方 Hugging Face 在本机网络超时；验证可达镜像后只在 ignored 本地环境临时设置 `HF_ENDPOINT`。Xet 日志权限警告不影响备用源下载，缓存后离线加载成功。
5. Codex 自动化 WSL 会话中 Docker service 在约 43--49 秒被外层停止；三种 keepalive 均失败。停止继续堆 workaround，保留失败证据，并把完整 Compose restart smoke 放到 Task 9 的稳定交互式终端门禁。
6. 提交前误把 Compose 专用 DSN 加载到 WSL 主机 pytest：`postgres` 只在 Docker 网络内可解析，数据库测试在连接准备阶段失败；改为容器网络内运行聚焦门禁。异常堆栈回显了本地开发凭据，因此立即轮换数据库角色密码并同步 ignored 根环境文件，未把旧值或新值写入 Git/文档。
7. 新建空 PostgreSQL 卷暴露迁移测试的隐式前置条件：测试直接执行 head→0004，却只依赖未迁移的 `database_url`。RED 为 1 failed/74 passed；改为显式依赖 `migrated_database_url` 后，单测 GREEN 1 passed，完整聚焦门禁 GREEN 75 passed。

## 新鲜证据

- 新建空卷容器网络聚焦测试 75 条；产品测试 535 条；Git worktree 安全测试 4 条。
- Ruff、173 文件格式、mypy 73 个源文件、114 包锁文件和仓库安全扫描通过。
- Compose/release Compose 配置通过，新 API/MCP/bootstrap 镜像构建成功；本次启动观察到 PostgreSQL/MCP healthy 与 API started。
- 三份 SOP 共 12 chunk；真实入库第二次三文档均 replay；三场景代表查询均只召回自身 v1.0.0 文档。

## 下一步

提交 Task 6 原子变更。随后进入 Task 7：持久化脱敏 Agent Trace，补齐 API/SSE/RBAC；完整 Compose 重启门禁仍保持待验证，生产发布门禁继续 `CLOSED`。

## Task 7：Agent Trace、API/SSE 与 RBAC

### 已完成

- 新增 `0006_agent_trace` 和 `agent_runs`、`agent_trace_events`、`agent_trace_citations`；使用行锁、递增序列与 `(run_id, semantic_key)` 唯一约束控制并发追加和恢复重放。
- TraceRecorder 从真实调查状态投影 perception、model、tool、rag、rule、guardrail、human、execution、feedback，不伪造或保存隐藏思维链。
- 递归脱敏限制字段、深度、集合与文本长度；RAG 只保存 citation reference，不复制 SOP 或工具正文。
- FastAPI 新增 Trace snapshot 与 SSE snapshot replay；operator、approver、auditor 和显式本地 demo-admin 按 operation 状态与 owner 做权限判断。
- 缺失 checkpoint 的真实 LangGraph 恢复测试证明 event ID 与 sequence 不变；人工审批、批准后执行和最终状态均可追溯。

### RED/GREEN 与问题复盘

1. 初始 RED 因 TraceRecorder 模块不存在而失败；实现后脱敏、仓储、API 和恢复测试逐步转绿。
2. 缺失库存对象原本在主体工具失败后继续 replan，Mock planner 生成空计划并返回 503；修复为主体/策略工具失败立即终止并传播原始稳定错误码，API 恢复 422 契约。
3. 把所有旧 API 测试默认切到 Agent 模式导致冻结用例 `RPL-024` 从 failed 变成 aborted；根因是测试 harness 扩大作用域。改为旧套件保留确定性基线，新 Trace 测试显式启用 Agent 模式。
4. WSL 不能访问 Windows `127.0.0.1:55432`，而 Windows PostgreSQL 又缺少 pgvector；最终在 Compose pgvector 容器网络执行定向测试。
5. Windows `uv` 误操作 Linux `.venv`，造成可执行目录部分缺失；随后由 WSL `uv sync --frozen` 按锁文件重建。后续禁止不同操作系统共用同名虚拟环境。
6. 数据库连接失败 traceback 展开了 Windows 本地测试角色旧密码；立即轮换并以新凭据验证，Git 和文档不记录任何值。

### 新鲜证据与边界

- 产品代码全量：`545 passed in 187.62s`。
- 最新 RBAC 补强后 Task 7 定向：`8 passed in 27.39s`。
- WSL Git 安全：`4 passed in 0.30s`。
- Ruff、181 个文件格式、mypy 77 个源文件、仓库安全扫描和 `git diff --check` 通过。
- 完整 Compose API/MCP 重启、真实 Kimi Trace、浏览器 Agent 工作台仍属于 Task 8--9，发布门禁继续 `CLOSED`。

### 下一步

提交 Task 7 原子变更，随后执行 Task 8：以有限业务表单、结构化 Goal、真实 Agent Trace、MCP/RAG 证据、审批绑定、Verifier 和下一角色引导重构 React 单页工作台。

## Task 8：React Agent 工作台

### 已完成

- 新增五个独立组件：Intent/Goal、Agent Trace、工具与引用、建议/计划对照、下一角色引导。
- API client 接入后端 Trace snapshot；Trace 权限失败不会清空同一 operation 已加载业务结果，终态由 auditor 接管读取。
- App 改为单页流程：表单 → Goal → Tool/RAG → 规则 → 审批 → Verifier → 工单；业务审计保持独立。
- 审批区展示规则版本和绑定哈希摘要，说明批准后绕过缓存重新取证；切换新 binding 会重置本地按钮状态。
- 视觉采用深色运营工作台、轻量渐变和响应式网格，无 fixed/sticky、无开放聊天框、无生成工具品牌字样。

### RED/GREEN 与视觉复盘

1. RED 证明五个组件和 `getAgentTrace` 均不存在；实现后聚焦 20 条通过。
2. citation 百分比首次测试因 JSX 文本节点分段而失败；改为核对同一元素完整 `textContent`，未改业务输出。
3. App 中模型建议同时出现在 Trace 与决策对照区，旧断言错误要求全页面唯一；改为至少一处，保留两个视角。
4. 390 宽度首版把七阶段流程条排成单列，占用高度过多；调整为两列后复核，无横向溢出。
5. 内置浏览器 full-page 长图出现重复拼接，但 DOM snapshot、普通 screenshot 和几何检查均只有单一页面；识别为工具拼接现象，没有修改产品代码掩盖。

### 新鲜证据与边界

- 前端：17 个测试文件、46 条测试通过。
- TypeScript build 与 Vite production build 通过。
- 1440/1024/390 响应式检查：横向溢出 0，应用 fixed/sticky 0。
- 当前视觉检查是未认证空状态；Task 9 才执行真实后端浏览器闭环、Compose 重启与 Kimi Trace。

### 下一步

原子提交 Task 8，随后进入 Task 9 的透明轨迹评测与真实 Compose 端到端门禁。
